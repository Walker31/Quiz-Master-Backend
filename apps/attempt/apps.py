from django.apps import AppConfig


class AttemptConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.attempt'
    label = 'attempt'
    verbose_name = 'Attempt'
